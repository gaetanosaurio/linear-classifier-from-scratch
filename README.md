# linear-classifier-from-scratch
Softmax regression / linear classifier implemented entirely from scratch using NumPy.

Demonstrates low-level understanding of forward passes, cross-entropy loss formulation, and vectorized backpropagation (gradient descent) without relying on PyTorch autograd.
